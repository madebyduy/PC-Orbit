using System.Windows;

namespace PcOrbit.App;

/// <summary>
/// The desktop face of the same engine <c>pco</c> drives: identical composition root, identical
/// data, identical transaction semantics. Only the surface differs (spec 18.2).
/// </summary>
public partial class App : Application
{
}
